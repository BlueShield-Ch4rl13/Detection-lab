# Generado por tools/build.py — no editar a mano
# Dominio: red  ·  ES|QL (Elastic)

# Patron de beaconing hacia un destino externo  [medium]
from * metadata _id, _index, _version | where `c-uri` like "*/*" and not (`cs-categories` in ("Business", "Search Engines", "Software Updates"))

# Canal de mando sobre un servicio legitimo  [high]
from * metadata _id, _index, _version | where `c-uri` like "*discord.com/api/webhooks*" or `c-uri` like "*discordapp.com/api/webhooks*" or `c-uri` like "*hooks.slack.com/services*" or `c-uri` like "*api.telegram.org/bot*" or `c-uri` like "*pastebin.com/raw*" or `c-uri` like "*hastebin.com/raw*" or `c-uri` like "*ghostbin.co/paste*" or `c-uri` like "*transfer.sh/*" or `c-uri` like "*anonfiles.com/*" or `c-uri` like "*gofile.io/uploadFile*" or `c-uri` like "*.ngrok.io*" or `c-uri` like "*.ngrok-free.app*" or `c-uri` like "*.trycloudflare.com*" or `c-uri` like "*.loca.lt*" or `c-uri` like "*.serveo.net*"

# Tunelizacion o exfiltracion por DNS  [high]
from * metadata _id, _index, _version | where query_type in ("TXT", "NULL") or query rlike "^[A-Za-z0-9+/=_-]{45,}\\..*"

# Interaccion con webshell  [critical]
from * metadata _id, _index, _version | where (`c-uri` like "*.php*" or `c-uri` like "*.jsp*" or `c-uri` like "*.jspx*" or `c-uri` like "*.aspx*" or `c-uri` like "*.ashx*") and (`c-uri` like "*cmd=*" or `c-uri` like "*exec=*" or `c-uri` like "*shell=*" or `c-uri` like "*passthru(*" or `c-uri` like "*system(*" or `c-uri` like "*whoami*" or `c-uri` like "*net user*" or `c-uri` like "*powershell*" or `c-uri` like "*/bin/bash*" or `c-uri` like "*eval(*")

# Volumen de subida anomalo hacia el exterior  [medium]
from * metadata _id, _index, _version | where (`cs-method` in ("POST", "PUT")) and not (`c-uri` like "*sharepoint.com*" or `c-uri` like "*onedrive.live.com*" or `c-uri` like "*.midominio.local*")
